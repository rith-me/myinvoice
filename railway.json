{
    "build": {
      "builder": "NIXPACKS_PHP",
      "config": {
        "phpVersion": "8.1",
        "documentRoot": "/public",
        "baseImage": "ubuntu-22.04",
        "buildCommand": "composer install --optimize-autoloader --no-dev && npm install && npm run production"
      }
    },
    "deploy": {
      "startCommand": "php artisan storage:link && chmod -R 775 storage bootstrap/cache && php artisan migrate --force && php artisan serve --host=0.0.0.0 --port=${PORT}"
    }
  }
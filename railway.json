{
    "$schema": "https://railway.app/railway.schema.json",
    "build": {
      "builder": "nixpacks",
      "config": {
        "phpVersion": "8.2",
        "nodeVersion": "18",
        "documentRoot": "public"
      },
      "buildCommand": "composer install --optimize-autoloader --no-dev && npm install && npm run production"
    },
    "deploy": {
      "startCommand": "bash -c 'php artisan storage:link && chmod -R 775 storage bootstrap/cache && php artisan migrate --force && php artisan serve --host=0.0.0.0 --port=${PORT}'",
      "healthcheckPath": "/",
      "healthcheckTimeout": 30
    }
  }